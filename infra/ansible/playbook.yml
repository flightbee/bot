---
- name: Deploy bot in Docker on remote server
  hosts: all
  become: true
  vars:
    bot_dir: /opt/bot
    terraform_env_path: "/opt/bot/.env"
  
  roles:
    - role: ssh
    - role: dependencies
    - role: docker
    - role: bot