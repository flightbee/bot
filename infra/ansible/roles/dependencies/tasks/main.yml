---
- name: Install required packages
  apt:
    name:
      - docker.io
      - docker-compose
      - git
      - python3-pip
    state: present
    update_cache: yes

- name: Add user to docker group
  user:
    name: "{{ ansible_user }}"
    groups: docker
    append: yes
